import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
      primary: {
        main: '#3B37DA'
      },
      success: {
        main: '#4caf50'
      },
    //   secondary: {

    //   }
    }
  })

  export default theme;
